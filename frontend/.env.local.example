# Harper Chat Service (FastAPI) base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Optional: Bearer token for API when HARPER_JWT_JWKS_URL is set (local smoke tests only).
# Prefer setting localStorage key "harper_access_token" after IdP login — see src/lib/api.ts
# NEXT_PUBLIC_ACCESS_TOKEN=
